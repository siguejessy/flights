<%- include('../partials/header') %>
<h1>Flight Details</h1>

<section id="show-page">
    <div>Flight: </div>
    <div><%= flight.airline %></div>
    <div>Departure Airport: </div>
    <div><%= flight.airport %></div>
    <div>Flight Number: </div>
    <div><%= flight.flightNo %></div>
    <div>Departure Time: </div>
    <div><%= flight.departure %></div>
    <div>Did you book the ticket?: </div>
    <div><%= flight.booked ? 'Yes' : 'Nope' %></div>
  </section>

<<<<<<< Updated upstream
=======
  <br><br><h2>Destinations</h2>
  
  <form id="add-destination-form" method="POST"
    action="/flights/<%= flight._id %>/destinations">
    <label>Destination:</label>
    <select name="airport">
      <option value="AUS">Austin TX, USA</option>
      <option value="SAL">San Salvador, ES</option>
      <option value="MEX">CDMX, MX</option>
      <option value="CDG">Paris, FR</option>
      <option value="PDX">Portland OR, USA</option>
    </select>
    <label>Arrival Date:</label>
    <input type="date" name="arrival">
    <input type="submit" value="Add Destination">
  </form>
  

<% if (flight.destinations) { %>
  <table>
    <thead>
      <tr>
        <th>Date</th>
        <th>Airport</th>
        <th>Arrival</th>
      </tr>
    </thead>
    <tbody>
      <% flight.destinations.forEach(function(d) { %>
        <tr>
          <!-- .toLocaleDateString() provides a more friendly date format -->
          <td><%= d.createdAt.toLocaleDateString() %></td>
          <td><%= d.airport %></td>
          <td><%= d.arrival %></td>
        </tr>
      <% }); %>
    </tbody>
  </table>
<% } else { %>
  <h5>No Destinations added yet</h5>
<% } %>

>>>>>>> Stashed changes
<%- include('../partials/footer') %>